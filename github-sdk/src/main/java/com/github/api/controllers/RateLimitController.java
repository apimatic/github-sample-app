/*
 * GitHubAPILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.github.api.controllers;

import com.github.api.ApiHelper;
import com.github.api.Server;
import com.github.api.exceptions.ApiException;
import com.github.api.exceptions.RateLimit404ErrorException;
import com.github.api.http.request.HttpMethod;
import com.github.api.models.RateLimitResponse;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class RateLimitController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public RateLimitController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * **Note:** Accessing this endpoint does not count against your REST API rate limit. **Note:**
     * The `rate` object is deprecated. If you're writing new API client code or updating existing
     * code, you should use the `core` object instead of the `rate` object. The `core` object
     * contains the same information that is present in the `rate` object.
     * @return    Returns the RateLimitResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public RateLimitResponse rateLimitGet() throws ApiException, IOException {
        return prepareRateLimitGetRequest().execute();
    }

    /**
     * **Note:** Accessing this endpoint does not count against your REST API rate limit. **Note:**
     * The `rate` object is deprecated. If you're writing new API client code or updating existing
     * code, you should use the `core` object instead of the `rate` object. The `core` object
     * contains the same information that is present in the `rate` object.
     * @return    Returns the RateLimitResponse response from the API call
     */
    public CompletableFuture<RateLimitResponse> rateLimitGetAsync() {
        try { 
            return prepareRateLimitGetRequest().executeAsync(); 
        } catch (Exception e) {  
            throw new CompletionException(e); 
        }
    }

    /**
     * Builds the ApiCall object for rateLimitGet.
     */
    private ApiCall<RateLimitResponse, ApiException> prepareRateLimitGetRequest() throws IOException {
        return new ApiCall.Builder<RateLimitResponse, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/rate_limit")
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .authenticationKey(BaseController.AUTHENTICATION_KEY)
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .deserializer(
                                response -> ApiHelper.deserialize(response, RateLimitResponse.class))
                        .nullify404(false)
                        .localErrorCase("404",
                                 ErrorCase.create("Resource not found",
                                (reason, context) -> new RateLimit404ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}